# Deploy

This project is a static site. Do not use a custom Pages workflow.

1. Push files to `main` with `index.html` in repository root.
2. GitHub → Settings → Pages.
3. Source: `Deploy from a branch`.
4. Branch: `main`.
5. Folder: `/(root)`.
6. Save.
